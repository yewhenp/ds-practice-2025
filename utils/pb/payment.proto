syntax = "proto3";

package bookstore;

import "commit_protocol.proto";

service PaymentService {    
    rpc Prepare (BookDataMessage) returns (CommitStatus);
    rpc Commit (BookDataMessage) returns (CommitStatus);
    rpc Abort (BookDataMessage) returns (CommitStatus);
}
